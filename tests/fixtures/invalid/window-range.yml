version: 1.1
source: samples.tpch.orders
fields:
  - name: order_date
    expr: o_orderdate
measures:
  - name: rolling_orders
    expr: COUNT(*)
    window:
      - order: order_date
        range: trailing 7 banana
        semiadditive: last
