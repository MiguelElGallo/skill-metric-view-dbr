version: 1.1
source: samples.tpch.orders
parameters:
  - name: optional_first
    data_type: int
    default: 1
  - name: required_after_default
    data_type: int
fields:
  - name: duplicate_name
    expr: o_orderkey
  - name: duplicate_name
    expr: o_custkey
measures:
  - name: duplicate_name
    expr: COUNT(*)
materialization:
  mode: strict
  schedule: TRIGGER ON UPDATE
  materialized_views:
    - name: empty_aggregate
      type: aggregated
