# Third-party notices

The bundled checker includes the following dependency:

| Package | Version | License | Source |
| --- | --- | --- | --- |
| `yaml` | 2.9.0 | ISC | https://github.com/eemeli/yaml |

The complete license text is distributed at `licenses/yaml-ISC.txt`.

The Model Context Protocol server implementation in this plugin is original, dependency-free adapter code. The `@modelcontextprotocol/sdk` package is used only by repository tests and is not included in the installed bundle.
